name: GitHub to Discord
on:
  push:
    branches: [main]  # Отправлять уведомления только из этих веток  

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Embed to Discord
        run: |
          curl -X POST "$DISCORD_WEBHOOK" \
          -H "Content-Type: application/json" \
          -d '{
            "username": "🛠️D2_MODDING_UPDATES",
            "avatar_url": "https://i.imgur.com/BCTrM9E.png",
            "embeds": [{
              "title": "🚀 Новое обновление репозитория!",
              "description": "[${{ github.event.pull_request.title }}](${{ github.event.head_commit.url }})\n${{ github.event.pull_request.body }}",
              "color": 11378687,
              "fields": [],
              "author": {
                "name": "${{ github.event.head_commit.author.name }}",
                "url": "https://github.com/withvoidwithin/dota2_modding"
              },
              "footer": {
                "text": "GitHub Actions | ${{ github.event.head_commit.timestamp }}"
              },
              "timestamp": "${{ github.event.head_commit.timestamp }}"
            }]
          }'
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}